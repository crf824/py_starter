# Python 学习方案（求职向：AI Agent + 数据分析）

> 调整说明：原方案是"语法逐课喂"，你反馈太慢。本方案改为 **目标倒推、项目驱动、用到再学**——把 Python 当工具，而不是当学科来通读。
> 贯穿项目：用你手上的 A股数据（pnl、板块、后续 tdx 行情）做一条「数据获取 → 清洗 → 分析 → 可视化 → Agent 自动复盘」流水线，真实可写进简历。

## 一、先回答：Python 最核心的是什么？

对你"AI Agent + 数据分析 + 找工作"的目标，核心**不是**"学完所有语法"，而是下面四块（按重要度）：

| 优先级 | 核心能力 | 为什么是核心 | 你现在进度 |
|---|---|---|---|
| 🔴 P0 | 基础语法够用（变量/函数/列表/字典/循环/条件/文件/异常/模块） | 一切地基，但不用精通 | ✅ 已完成约 70%（lesson1~17） |
| 🔴 P0 | **Pandas + NumPy** | 数据分析命根子，岗位 JD 必写，每天在用 | ⚪ 还没碰 |
| 🟡 P1 | 数据获取与清洗（CSV/Excel/JSON/API/SQL） | 真实工作 80% 时间在搞数据 | ⚪ 仅 CSV 浅尝 |
| 🟡 P1 | 调 API + JSON（requests） | AI Agent 本质 = 调 API + 拼逻辑 | ⚪ 未碰 |
| 🟢 P2 | LLM SDK + function calling + 自建 agent 循环 | 你的终点目标 | ⚪ 未碰 |
| 🟢 P2 | 工程化（CLI/pytest/FastAPI/作品集） | 求职差异化 | ⚪ 部分（git 会了） |

## 二、可以"暂时不碰"的清单（省时间的关键）

别在入门阶段耗时间，用到再说：
- 深 OOP：类继承、多态、元类、描述符
- 装饰器、生成器高级用法
- 多线程/多进程底层原理
- 复杂算法与数据结构（面试前再刷）
- 正则深究、GUI（tkinter）、网络底层（socket）

> 你能"**用类**"就够了（会用 Pandas/requests 这些库），不需要"**会设计类**"。

## 三、新路线图（项目驱动）

| 阶段 | 主题 | 关键技能 | 实战产出 |
|---|---|---|---|
| 1 | 数据处理双引擎 | NumPy 数组/向量化；Pandas Series/DataFrame/`read_csv`/`groupby`/`merge`/时间序列 | 用 `df.groupby('sector')['pnl'].sum()` 重写第17课归因图（甩掉手搓 dict） |
| 2 | 真实数据获取与清洗 | `requests` 调 API、JSON 解析、读 Excel/SQL、缺失值/类型/去重 | 拉一份行情/板块数据，清洗成干净 DataFrame |
| 3 | 可视化进阶 | seaborn / plotly EDA、交互图 | 板块收益 + 净值的交互仪表盘 |
| 4 | 走向 AI Agent | LLM SDK（DeepSeek 兼容）、streaming、function calling、自建 agent 循环、async 基础 | 写"读交易 CSV → 调大模型 → 输出复盘报告"的小 Agent |
| 5 | 工程化与求职 | argparse CLI、pytest、FastAPI 包装、作品集 + 面试刷题 | 可演示的"A股分析 Agent"小项目 + README |

## 四、学习原则调整

- ❌ 不再"一课一个语法点慢慢喂"
- ✅ **每阶段 = 一个小项目**，遇到不会的语法当场补（用到再学）
- ✅ 继续保留 🐍/🌐 前端对照（你说过好用）
- ✅ 节奏由你控：每阶段结束你看一眼成果，决定快慢

## 五、当前进度

- ✅ 决策已定：**均衡路线（数据 + Agent 都要）**，**直接上 Pandas** 起步。
- 🟢 阶段1 进行中：**第18课 用 Pandas 一行重写板块归因**已完成（结果同第17课，但 8 行手搓 → 1 行 groupby）。
- 下一步：阶段1 继续——NumPy 向量化、DataFrame 更多操作（筛选/merge/时间序列），用你的 A股 数据做更多实战。
